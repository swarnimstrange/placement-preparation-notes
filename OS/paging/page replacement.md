FIFO -> the page that came first will go out first.

LRU -> the page which is least recently used will go out first.

Optimal page replacemnet -> we replace that page which is not used much in future.